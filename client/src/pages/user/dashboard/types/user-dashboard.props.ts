import type { LoadingComponentProps } from '../../../../shared/abstract/loading-component';
import type { AxiosPrivateProps, AuthProps } from '../../../../hooks';

export interface UserDashboardProps extends AxiosPrivateProps, AuthProps, LoadingComponentProps {
	
}
